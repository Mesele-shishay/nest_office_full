/**
 * Common Components Barrel Export
 * 
 * This directory contains shared components that are used across multiple
 * sections of the application but don't belong to a specific domain.
 * 
 * Components in this directory:
 * - pagination.tsx: Reusable pagination component for data lists
 * 
 * Usage: import { PagePagination } from '@/components/common'
 */

export { PagePagination } from './pagination'
