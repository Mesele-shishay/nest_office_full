export enum Role {
  ADMIN = 'admin',
  MANAGER = 'manager',
  USER = 'user',
  // Hierarchical admin roles
  CITY_ADMIN = 'city_admin',
  STATE_ADMIN = 'state_admin',
  COUNTRY_ADMIN = 'country_admin',
}
